import React from 'react';
import ReactDOM from 'react-dom/client';
import { SigaTutoriasApp } from './SigaTutoriasApp';
import 'bootstrap/dist/css/bootstrap.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <SigaTutoriasApp />
    </React.StrictMode>
)